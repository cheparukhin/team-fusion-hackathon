# Foldseek raw-score observation

The first qualifying actual-model spans were two overlapping Pfam annotations in the same contextual control (`control_4d8da7a249b8e2edf2d48ecc95d93eb6898532d6e07ed982fec0fc4c9c14e12a`): PF04670.19 at residues 19–141 and PF00071.29 at 19–168. Their coordinate overlap is identical by construction because both are extracted from the same validated model. They are not two independent biological observations.

The installed Foldseek binary reported raw `alntmscore` values 1.007–1.008 for self/nested alignments in this snapshot, slightly above the theoretical unit upper bound of a TM-score. Raw tool output is preserved without silently clipping values. This appears to reflect the tool's numerical or normalization behavior; its exact cause has not been established. The identity geometry and >=0.82 coverage make acceptance at the preregistered 0.5/0.8 thresholds unambiguous, but these values should not be presented as calibrated scores exceeding a perfect match. One connected component here is a control-only cluster; it is not evidence of candidate fold diversity. Future near-threshold cases with this behavior should be independently checked before interpreting cluster membership.
