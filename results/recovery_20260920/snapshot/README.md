# Selected historical source files

These files are byte-for-byte copies from the recovered quick_hack workspace. Read [the reconciliation report](../README.md) first. This is a compact evidence snapshot, not a standalone runnable campaign. Source paths, full-archive destinations and hashes are in [the disposition manifest](../file_dispositions.tsv). No old status, compute command or policy in this snapshot authorizes restarting an expired job.

Use the local archive for model coordinates, complete report assets, score profiles and other omitted inputs. Current reviewed submission claims remain in the canonical repository outside this snapshot.
